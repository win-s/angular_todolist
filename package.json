{
  "name": "todolist-angular1",
  "private": true,
  "version": "0.0.1",
  "description": "Small application AngularJS 1",
  "repository": "https://github.com/win-s/angular_todolist.git",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "update-deps": "npm update",
    "postupdate-deps": "bower update",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  }
}
